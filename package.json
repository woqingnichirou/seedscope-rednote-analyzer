{
  "name": "seedscope",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered Rednote Competitive Content Analyzer",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "lint": "npm --prefix apps/web run lint"
  },
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=20"
  }
}
